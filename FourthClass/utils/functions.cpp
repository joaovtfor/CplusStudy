#include <iostream>
#include <iomanip>

using namespace std;

int adiciona(int x, int y) {
    int t = x + y;
    return t;
}

int subtrai(int x, int y) {
    int t = x - y;
    return t;
}

int multi(int x, int y) {
    int t = x * y;
    return t;
}

int division(int x, int y) {
    int t = x / y;
    return t;
}